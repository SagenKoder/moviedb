# Production Frontend Environment Variables (React/Vite)
# These variables are embedded into the built JavaScript during build time
# For production deployment, copy this to .env.production and update values

VITE_AUTH0_DOMAIN=your-domain.auth0.com
VITE_AUTH0_CLIENT_ID=your-production-client-id
VITE_AUTH0_AUDIENCE=https://your-production-api.com
VITE_API_BASE_URL=/api